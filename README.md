# Test

Simple repository.

## Usage

Run `python3 audio_signal.py` to generate a sine wave audio signal and save a graph of the signal, its amplitude, and the exponential moving average to `signal.png`.
